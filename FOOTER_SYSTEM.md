# Footer System

Batch 12 adds a reusable global footer system without touching `index.html`.

## Files

- `js/site-footer.js`
- `data/sitewide-footer.json`
- `partials/footer.html`
- `footer.html`

The footer is injected/enhanced client-side so the static site can keep one reusable footer pattern without adding a build step.
