# Placeholder only.
# This script intentionally does not modify files automatically yet.
# Add real URLs/provider values to data/*.json first, then create a focused patch script.

Write-Host "Provider config apply script is a placeholder." -ForegroundColor Yellow
Write-Host "Do not auto-apply unknown URLs. Confirm real values first." -ForegroundColor Yellow
