# Broken Link Audit

Use the local audit script:

```powershell
& ".\scripts\link-audit.ps1"
```

The script checks internal local links in HTML files.

External links are not validated by this local script and should be manually reviewed.
